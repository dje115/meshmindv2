# MeshMind v2 Monorepo Scripts

Task runner scripts for the monorepo.

- Use `make` (see root Makefile) or `.\packages\scripts\run.ps1` on Windows
- Root `scripts/` holds env checks, bootstrap; this package holds app-level tasks
